import React from 'react';
import './styles.css'

const Loading = () => {
    const classes = 'loading fade-out'
    return (
        <div className={classes}>
            <h1>
                Loading
            </h1>
        </div>
    );
};

export default Loading;